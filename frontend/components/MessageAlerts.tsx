'use client';
import React from 'react';

/**
 * Placeholder component kept temporarily to avoid Tailwind/Turbopack ENOENT during active dev session
 * when a file is deleted and Tailwind tries to stat it as a "changed file".
 * Safe to delete after restarting the dev server.
 */
const MessageAlerts = () => null;

export { MessageAlerts };


